/* 表情原图配置文件 */
// import emojiObj from './emoji/emoji.json'
import bearObj from './bear/bear.json'

// 过滤掉没有unicode的表情
// console.log(emojiObj)
// emojiObj.data = emojiObj.data.filter(item => item.unicode)
export default {
  // emoji: emojiObj,
  bear: bearObj
}
